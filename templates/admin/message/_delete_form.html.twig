<button class="btn btn-danger btn-sm" data-toggle="modal" data-target="#deleteMessageModal">
    Изтриване
</button>

<div class="modal fade" id="deleteMessageModal" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Изтриване на съобщение</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p class="m-0">Сигурни ли сте, че искате да изтриете съобщението?</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Отказ</button>
                <form class="d-inline-block form-inline"
                      method="post"
                      action="{{ path('admin_message_delete', {'id': message.id}) }}">
                    <input type="hidden" name="_method" value="DELETE">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ message.id) }}">
                    <button type="submit" class="btn btn-danger">Изтриване</button>
                </form>
            </div>
        </div>
    </div>
</div>
